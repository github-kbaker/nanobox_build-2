[phases.setup]
providers = ["python"]

[phases.install]
dependsOn = ["setup"]
cmds = ["pip install -r requirements.txt"]

[phases.build]
dependsOn = ["install"]

[start]
cmd = "uvicorn server:app --host 0.0.0.0 --port $PORT"